import Foundation

// It represent the name of a request.
// It is used as a key value on the mapping request-manager.
public enum CoordinationRequestName: String {

    /// This case list is potentially lengthy,
    /// so please keep it sorted alphabetically.
    case dismiss
    case showLectureDetails
}
